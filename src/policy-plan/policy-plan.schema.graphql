scalar DateTime

type PolicyPlan {
    id: Int!
    name: String!
    ceiling: String!
    policyId: Int!
    createdAt: DateTime
    updatedAt: DateTime
    totalActiveIssuedPlansCount: Int
}

type Query {
    getAllPolicyPlan: [PolicyPlan]!
}
