
type MemberMedicalProfile {
    id: Int!
    memberId:Int!
    member:Member
    staffId:string
    age:Int
    height:Int
    trs:Int
    bmi:Int
    createdAt: DateTime
    updatedAt: DateTime
}
type Query {
    listMembersMedicalProfile: [MemberMedicalProfile]!
    listMemberMedicalProfileinfo(id: Int): MemberMedicalProfile
}
